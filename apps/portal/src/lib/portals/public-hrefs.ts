/**
 * DEPRECATED location - this module moved to
 * `@airegistry/public/lib/public-hrefs`.
 *
 * Re-export shim kept while the deprecation window is open. Remove this file
 * (and any `@/lib/portals/public-hrefs` importers) once everything has been
 * migrated.
 */
export { resolveProviderPortalPublicHref } from "@airegistry/public/lib/public-hrefs";
