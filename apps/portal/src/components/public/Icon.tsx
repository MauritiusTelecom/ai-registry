/**
 * DEPRECATED — `Icon` has moved to `@airegistry/ui-kit`.
 *
 * This file is a re-export shim left behind because the workspace sandbox
 * could not delete files during the PR-1 migration (see MIGRATION.md, same
 * convention used for `src/` cleanup). The portal codebase no longer imports
 * from this path; remove this file when the deprecation window closes.
 */
export { Icon, type IconName } from "@airegistry/ui-kit";
