/**
 * DEPRECATED location - this hook moved to `@airegistry/public/shell/useCountUp`.
 *
 * Re-export shim retained so any importer still using
 * `@/components/public/useCountUp` keeps resolving during the deprecation
 * window. Remove this file once the deprecation window closes.
 */
export { useCountUp } from "@airegistry/public/shell/useCountUp";
