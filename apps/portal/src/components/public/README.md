# Deprecated — edit `@airegistry/public` instead

Public site UI lives in [`packages/public`](../../../../packages/public/). Route shims are under [`apps/portal/src/app/(public)/`](../../app/(public)/).

Do not add or edit components in this folder. It exists only until any remaining `@/components/public/*` imports are removed.
