/**
 * @airegistry/ui-kit — placeholder.
 *
 * This package will host the headless components and design tokens lifted
 * from ai-registry-prototype/. For now it ships only the tokens.css stub
 * so the portal can import it and operators can override CSS variables
 * without forking the portal.
 *
 * Roadmap:
 *   - tokens.css: full token set (colors, spacing, type, radii)
 *   - Components: Button, Badge, Card, Modal, Tabs, etc.
 *   - <PluginSlot id="..."> primitive so extensions can mount UI here.
 */

export const UI_KIT_VERSION = "0.1.0" as const;
