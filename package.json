{
  "name": "airegistry",
  "version": "0.1.0",
  "private": true,
  "description": "Sovereign AI Registry — Prisma DDL (registry schema) aligned with airegistry-specs",
  "scripts": {
    "prisma:generate": "prisma generate",
    "prisma:validate": "prisma validate",
    "prisma:format": "prisma format",
    "prisma:migrate": "prisma migrate deploy"
  },
  "prisma": {
    "schema": "src/prisma/schema.prisma"
  },
  "dependencies": {
    "@prisma/client": "^6.19.3"
  },
  "devDependencies": {
    "prisma": "^6.19.3",
    "typescript": "^5"
  }
}
