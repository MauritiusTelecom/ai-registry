# This directory holds local-only secrets consumed by deploy.sh.
# It is gitignored — only this .gitkeep is tracked, so the directory exists
# after a fresh clone. Drop .env.production here (chmod 600).
